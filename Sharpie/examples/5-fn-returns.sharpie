fn makeSentence(int name int age) {
    return name "is" age "years old";
}

fn main() { 
    var string name = "Michael";

    for (index;1;17) 
        var string sentence = extract makeSentence(name index);
        log(sentence);
        newLine();
    for_end
}