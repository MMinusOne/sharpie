class User 
    var string balance = "1000";

    fn constructor (int bankBalance) {
     
    }

    fn setBalanace (int number) {
    }

    fn getBalance () {
        return balance;
    }
class_end

fn main() {
  var User user = new User();
  var string balance = user.balance;
  log(balance);

  var string balanceThruFunction = < user.getBalance() >;
  newLine();
  log(balanceThruFunction);
}